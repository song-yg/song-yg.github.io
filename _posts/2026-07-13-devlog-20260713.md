---
title: "개발일지 #1 - 이게 되나?"
date: 2026-07-13 18:00:00 +0900
categories: [개발일지, 사료축산뉴스봇]
tags: [python, jekyll, gdelt, 설계]
---

프로젝트 간략 소개

***사료 원료와 축산 분야의 뉴스를 광범위하게 긁어와***

***한눈에 비교하고 동향 파악이 가능한 주간 뉴스!***

를 만들고 있다.

어제-오늘 중으로 기획이 끝나고 코드 작성을 시작한다...

클로드와 함께!

---

## 종합 랭킹 삭제

이건 기획적인 부분이긴 하다.

핵심적인 기능 중 하나를 삭제했다.

<br>

국내외의 소스 크기 차이가 심해서 어떻게도 정규화를 하기 쉽지 않았다.

순수 갯수로만 따지자니 국내 버즈량이 너무 크고,

그렇다고 가중치를 주자니 너무 인위적이다!

솔직히 다 억지였다.

그래서 그냥 깔끔하게 포기했다.

```python
# scorer.py
def split_domestic_international(articles: list[dict]) -> tuple[list[dict], list[dict]]:
    """
    국내/해외 개별 집계 축 분리. 네이버 = 국내, WATT/GDELT = 해외
    """
    domestic = [a for a in articles if a.get("source") == "네이버"]
    international = [a for a in articles if a.get("source") != "네이버"]
    return domestic, international
```

국내외 Top N을 따로 보여주고,

동시에 나온 이슈는 🔗 표시를 붙이는 방향으로 결정했다.

좀 아쉽긴 한데, 별 수 없는 듯.

<br>

아, 🔗는 결정만 한거다.

아직 기능은 없다.

~~아니 잠깐만... 기사 원문 링크에 이 이모지 쓰는게 나은가?~~

그리고 아직도 리스트 컴프리헨션? 한 줄에 길게 쓰는 건 적응이 안 된다.

타입 힌트라는 것도 새로 배웠다.

---

## 언론사 중복 거름

같은 언론사가 같은 기사를 여러 번 내는 걸 걸러내려면 언론사 정보가 필요했다.

사실 혼자 기획할때는 못떠올렸었는데 클로드가 짚어줌.

<br>

근데...

네이버 검색 API에는 언론사명이 없다.

저작권은 어디에 팔아먹었지.

```python
# naver_collector.py
def _extract_press(originallink: str) -> str:
    """
    네이버 API 응답에는 언론사명 필드가 따로 없다.
    originallink(원문 URL)의 도메인을 언론사 식별자로 대신 사용한다.

    예: "https://www.yna.co.kr/view/AKR2026..." -> "yna.co.kr"
    """
    if not originallink:
        return ""
    domain = urlparse(originallink).netloc
    domain = domain.replace("www.", "")
    return domain
```

잠깐 당황하긴 했는데, 원문 URL에서 도메인만 뽑아와서 press 필드를 만드는 걸로 해결했다.

이게 도메인이구나.

오늘 처음 알았다.

~~고마워요 클로드웨건!~~

<br>

urllib.parse 라는 라이브러리를 사용했다.

파이썬 표준 라이브러리로, URL을 다룰 때 사용한다고 한다.

netloc로 호스트를 뽑고, www를 제거해 도메인만 남기는 방식으로 사용했다.

<br>

description도 이제부터는 같이 저장하기로 했다.

```python
# naver_collector.py - search_naver_news() 반환 구조
results.append({
    "source": "네이버",
    "title": _strip_html_tags(item["title"]),
    "url": item["originallink"] or item["link"],
    "published_at": _parse_pub_date(item["pubDate"]),
    "category": None,   # 카테고리는 정제(normalizer) 단계에서 채움
    "body": None,       # 네이버는 본문을 안 줌
    "description": _strip_html_tags(item["description"]),
    "press": _extract_press(item["originallink"]),
})
```

사실 모이고 있단 것도 잊고있었음.

## GDELT 반환 구조

GDELT는 기사랑 시계열 데이터가 성격이 완전히 달랐다.

시계열은 GDELT에서 자체적으로 제공하는 키워드별 언급량이라고 보면 된다.

참고하기 좋은 정보라 수집하는 방향으로 기획했다.

```python
# gdelt_collector.py
def collect(keywords: list[str] | None = None, skip_timeline: bool = False) -> tuple[list[dict], dict]:
    """
    반환값:
      articles: 공통 스키마 리스트. 다음 단계(정규화/이슈그룹핑)로 그대로 전달됨
      timeline: {키워드: {"vol": [...], "volraw": [...]}} 형태.
                스코어링에는 안 들어가고 결과물에 참고 지표로만 별도 표시
    """
```

그냥 (articles, timeline) 형태로 반환하도록 바꿨다.

데이터는 깔끔할수록 좋음...

---

## 정부기관 보도자료

처음 소스를 찾았을때는 실시간 반영 뉴스인줄 알고 기뻐했는데...

거의 6개월씩 딜레이가 되어 발표되는 자료였다.

...그래서 원래는 뺄까 했는데 클로드가 사후 매칭은 어떠냐고 했다.

<br>

흠? 낫베드.

<br>

안그래도 소스가 너무 적어서 걱정이었는데,

써먹을 방식이 있다니 다행이다.

그렇게 그룹핑이 끝난 이슈에만 사후 매칭하는 방식으로 방향을 바꿨다.

세부 구현은 아직 미정.

주축 데이터도 아니라서 일단 미뤘다.

---

## 할루시네이션 방지

기사 하나만 있는 이슈는 LLM 요약을 만들지 않기로 했다.

제목 하나 던져주고 요약해달라고 하면 결국 제목을 길게 늘여 쓰는 수준밖에 안 나온다.

그게 말 그대로 허위 기사... 잖아.

---

## 본문 추출 방식

`watt_collector`는 본문 위치를 정확히 찾기보다, 

추천 기사나 관련 기사 영역이 나오기 전까지의 `<p>` 태그만 수집하는 방식으로 구현했다.

<p>는 HTML에서 문단을 나타내는 태그라고 한다.

```python
# WATT_collector.py - _fetch_detail()
paragraphs = []
for p_tag in soup.find_all("p"):
    text = p_tag.get_text(strip=True)
    if not text:
        continue
    if text.lower() in ("recommended", "related stories"):
        break
    paragraphs.append(text)
body = "\n\n".join(paragraphs) if paragraphs else None
```

솔직히 좀...

노가다에...

맨 땅에 해딩하는 것 같아보였는데...

잘 돌아가면 된 거 아닌가?

---

## 제외할 사이트

### SunSirs

이용약관을 참고하라는데

이용약관이 없음.

~~내가못찾은거겠지만~~

### 국내 축산 전문지 4곳

축산신문, 축산경제신문, 농수축산신문, 농업축산신문.

전부 무단전재 금지, 일부는 AI 학습도 금지.

소스가 갑자기 우르르 보여서 신났었는데...

아쉽지만 보내줬다.

---

## GDELT도 조금 더 알아봤다

공식 문서를 뒤져봤다.

(클로드가)

rate limit은 있다고 한다.

근데 숫자는 안 알려준다.

...

<br>

그래도 몇 가지는 확인할 수 있었다.

```python
# gdelt_collector.py

MAX_RETRIES = 4
BACKOFF_BASE_SECONDS = 60  # 60 -> 120 -> 240 -> 480초로 증가 (누적 900초)

# 일시적 네트워크 에러(ConnectTimeout 등) 재시도 횟수/대기시간.
# 429(rate limit)와 ConnectTimeout이 같은 실행 안에서 섞여 나오는 걸 확인함.
# 이건 요청 제한과는 별개로, 순간적인 접속 실패일 가능성이 높아 429보다는 훨씬 짧게 재시도.
NETWORK_ERROR_MAX_RETRIES = 2
NETWORK_ERROR_WAIT_SECONDS = 10
```

* 공식적으로 요청 제한이 존재한다는 점
* 실사용 사례에서는 짧은 시간에 요청이 몰리면 약 15분 정도 차단되는 경우가 있다는 점(공식은 아님)
* TIMESPAN 파라미터 형식
* ConnectTimeout은 429와 별개로 처리하는 편이 맞겠다는 점

공식 문서보다 남의 삽질 기록이 훨씬 도움이 됐다.

역시 최고 스승은 누군가의 고통인 듯.

---

## 퇴근

일단 처음으로 코드 작성에 들어왔다.

지난 프로젝트는 저작권 이슈로 기획 단계에서 무산되었는데,

뭐라도 시작하니까 나은 듯!

이용약관 읽는게 진짜 눈알 빠지는 줄 알았다;;

## 다음에 할 일

* WATTAgNet, Feed Strategy, Feed & Grain 구조가 정말 같은지 직접 확인하기
* GitHub Actions에서 크롤링 차단이 있는지 테스트하기
* GDELT 429 대응 더 다듬기

음.

근데 나 왜 자꾸

일을 키우고 있지?