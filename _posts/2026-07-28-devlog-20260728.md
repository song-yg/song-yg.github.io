---
title: "개발일지 #11 - 3000분이요?"
date: 2026-07-28 12:00:00 +0900
categories: [개발일지, 사료축산뉴스봇]
tags: [python, 디버깅, github-actions]
---

어제 너무 피곤해서 집 가자마자 기절했더니...

눈 뜨니 출근임. ~~잠들기 전에 실행 한번 더하려 했는데 그것도 안함~~

☠️☠️☠️

<br>

---

## LLM 무료모델

어제 ANTHROPIC? 그 유료 말고 그냥 오픈라우터로 결정했다.

아무래도 매달 $1로 고급 하실래요 한번 $10로 일반 하실래요면...

사실 내가 후자 추천 드린 것도 맞긴 하다.

<br>

아무튼 그래서 그 부분 문서 수정하다보니까

모델 호출 실패하면 다른 모델 찾아가는 방어로직이 없었음...

있는줄 알았는데 실패가 아니라 모델 지정을 안한 상황만 해놨던거임!

<br>

```markdown
지정 모델로 호출 시도
    ↓ 실패
[모듈] 🟡 주의 - 지정 모델(...) 호출 실패 - openrouter/free 자동 라우팅으로 재시도
    ↓
openrouter/free로 재시도
    ↓ 성공하면 → 그대로 결과 사용 (그 배치/이슈는 정상 처리됨)
    ↓ 그것도 실패하면 → 기존처럼 안전하게 통과/생략 처리
```

그래서 추가함.

<br>

---

## 최대 6시간

키워드 아무 생각 없이

*깔\~~깔 이 중에 안걸리는게 있을리가!!!*

이러고 200개 다 켜놨더니 4시간 넘어서 끊김.

난 내가 시간을 더 늘려놓은 줄 알았어...

<br>

그래서 아예 냅다 8시간으로 늘리자! 했는데

![클로드](/assets/img/posts/2026-07-28-devlog/02-claude.png){: width="450" .normal }

...

**나 쁜 아 침**

<br>

```python
    # --- 시간 예산 추적 시작 ---
    # time.monotonic()은 시스템 시각 변경(NTP 보정 등)에 영향 안 받는 경과 시간 측정용 시계라 이런 용도에 적합.
    collect_start = time.monotonic()
    budget_exceeded = False
    skipped_due_to_budget: list[str] = []
 
    def _over_budget() -> bool:
        return (time.monotonic() - collect_start) >= TIME_BUDGET_SECONDS
```

```python
    for batch_idx, batch in enumerate(batches):
        if _over_budget():
            remaining = [kw for b in batches[batch_idx:] for kw in b]
            skipped_due_to_budget.extend(remaining)
            budget_exceeded = True
            print(f"[gdelt] 🟡 주의 - 시간 예산({TIME_BUDGET_SECONDS // 3600}시간) 소진"
                  f" - 남은 배치 {len(batches) - batch_idx}개(키워드 {len(remaining)}개)는 이번 실행에서 건너뜀. 다음 실행에서 다시 시도됨.")
            break
```

그래서 6시간으로 늘리고 gdelt 5시간 걸리면 다음으로 넘어가게 바꿨당...

하하 덕분에 방어로직 더 생기고 좋은거지~

~~안좋아 X발~~

<br>

---

## 깃허브 과금 유도 고발합니다.

대학생 최고 복지...

***깃허브 프로 무료 이용!***

~~인증서발급3000원은무시하기~~

...이었는데.

<br>

![깃허브](/assets/img/posts/2026-07-28-devlog/01-git.png){: width="450" .normal }

...

*난 내가 50시간(3000분)을 넘길지 몰랐지...*

아무튼 그래서 로컬에서 돌리는 중.

이것도 의외로 나쁘지 않다. 켜놓고 퇴근하지 못하는 빼곤... 하.

<br>

그래도 깃허브 사랑해~❤️

너 없으면 난 이미 짤렸어~🎶🎵🎶🎵

<br>

---