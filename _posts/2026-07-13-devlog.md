---
title: 개발일지 #1 - 이게 되나?
date: 2026-07-13 18:00:00 +0900
categories: [개발일지, 사료축산뉴스봇]
tags: [python, jekyll, gdelt, 설계]
---

오늘은 새로운 기능을 만들었다기보다는, 그동안 미뤄두던 설계들을 하나씩 정리한 날이었다.

분명 "결정만 하면 되는 일"이라고 생각했는데, 막상 하나를 정하면 다른 하나가 꼬이고...
그걸 다시 정리하다 보니 하루가 다 지나갔다. 역시 코드를 안 짜는 날도 개발은 개발이다.

## 종합 랭킹, 결국 포기했다

처음에는 국내(네이버)와 해외(WATT/GDELT)를 합쳐서 하나의 종합 랭킹을 만들려고 했다.

```python
# scorer.py
def split_domestic_international(articles: list[dict]) -> tuple[list[dict], list[dict]]:
    """
    국내/해외 개별 집계 축 분리. 네이버 = 국내, WATT/GDELT = 해외
    """
    domestic = [a for a in articles if a.get("source") == "네이버"]
    international = [a for a in articles if a.get("source") != "네이버"]
    return domestic, international
```

그런데 생각하면 할수록 이상했다.

네이버랑 WATT/GDELT는 애초에 수집 대상도 다르고 규모도 다른데, 점수를 하나로 합치는 순간 "왜 이 점수가 나왔는지" 설명하기가 어려워졌다.

정규화도 고민해봤고, 언론사 수를 제한하는 방법도 생각해봤는데 둘 다 억지 같았다.

결국 종합 랭킹은 없애기로 했다.

국내 Top N, 해외 Top N을 따로 보여주고, 두 곳에서 동시에 다뤄진 이슈만 🔗 표시를 붙이는 방식으로 정리했다.

(참고: 위 함수는 축을 나누는 부분까지만 구현된 상태다. 🔗 표시 자체는 2026-07-15 시점까지도 실제로는 구현되지 않고 있다 — 이슈 그룹핑이 붙어야 "어느 그룹이 국내·해외 양쪽에 걸치는지" 판단할 재료가 생기기 때문에, 그 전까지는 설계만 확정된 상태로 남아 있었다.)

가끔은 뭔가를 추가하는 것보다 없애는 게 더 좋은 설계인 듯.

그리고 사실 아직도 저 리스트 안 처럼 한 줄에 때려박는게 어색하다.

---

## 네이버 수집기에 필요한 정보도 추가하기로 했다

동일 언론사가 같은 기사를 여러 번 내는 경우를 걸러내려면 언론사 정보가 필요했다.

그런데 네이버 검색 API에는 언론사명이 없다.
저작권은 어디에 팔아먹었지.

```python
# naver_collector.py
def _extract_press(originallink: str) -> str:
    """
    네이버 API 응답에는 언론사명 필드가 따로 없다.
    originallink(원문 URL)의 도메인을 언론사 식별자로 대신 사용한다.

    예: "https://www.yna.co.kr/view/AKR2026..." -> "yna.co.kr"
    """
    if not originallink:
        return ""
    domain = urlparse(originallink).netloc
    domain = domain.replace("www.", "")
    return domain
```

잠깐 당황했지만, `originallink`에서 도메인을 추출해서 `press` 필드를 만드는 방식으로 해결하기로 했다.

그리고 `description`도 앞으로는 사용하기로 했다.

```python
# naver_collector.py - search_naver_news() 반환 구조
results.append({
    "source": "네이버",
    "title": _strip_html_tags(item["title"]),
    "url": item["originallink"] or item["link"],
    "published_at": _parse_pub_date(item["pubDate"]),
    "category": None,   # 카테고리는 정제(normalizer) 단계에서 채움
    "body": None,       # 네이버는 본문을 안 줌
    "description": _strip_html_tags(item["description"]),
    "press": _extract_press(item["originallink"]),
})
```

그동안은 열심히 수집만 해놓고 방치했는데, 이제는 LLM 요약 만들 때라도 써먹어야겠다.

---

## GDELT 반환 구조도 정리했다

GDELT는 기사 데이터와 시계열 데이터가 성격이 완전히 달랐다.

괜히 하나로 합쳐놓으면 나중에 내가 헷갈릴 것 같아서 `(articles, timeline)` 형태로 반환하기로 결정했다.

```python
# gdelt_collector.py
def collect(keywords: list[str] | None = None, skip_timeline: bool = False) -> tuple[list[dict], dict]:
    """
    반환값:
      articles: 공통 스키마 리스트. 다음 단계(정규화/이슈그룹핑)로 그대로 전달됨
      timeline: {키워드: {"vol": [...], "volraw": [...]}} 형태.
                스코어링에는 안 들어가고 결과물에 참고 지표로만 별도 표시
    """
```

미래의 나를 조금이라도 덜 괴롭히기 위한 선택이다.

---

## 정부기관 보도자료는 방향만 잡았다

정부기관 보도자료를 별도 수집기로 만드는 대신, 이미 그룹핑이 끝난 이슈에만 사후 매칭하는 방향으로 정리했다.

세부 구현은 아직 미정이다.

오늘은 "어떻게 만들지"보다는 "어떻게 안 만들지"를 더 많이 고민한 것 같다.

---

## 재료가 부족하면 요약도 하지 않기로 했다

기사 하나만 있는 이슈는 LLM 요약을 만들지 않기로 했다.

제목밖에 없는데 요약을 만들면 결국 제목을 길게 풀어쓰는 것밖에 안 된다.

그게 말 그대로 허위 기사... 잖아.

---

## 본문 추출 방식도 확정했다

`watt_collector`는 본문 컨테이너를 정확히 찾기보다, 추천 기사나 관련 기사 영역이 나오기 전까지의 `<p>` 태그만 수집하는 방식으로 구현했다.

```python
# WATT_collector.py - _fetch_detail()
paragraphs = []
for p_tag in soup.find_all("p"):
    text = p_tag.get_text(strip=True)
    if not text:
        continue
    if text.lower() in ("recommended", "related stories"):
        break
    paragraphs.append(text)
body = "\n\n".join(paragraphs) if paragraphs else None
```

처음에는 너무 대충 만들었나 싶었는데, 실제 기사 여러 개를 돌려보니 오히려 가장 안정적으로 동작했다.

괜히 복잡하게 만들 뻔했다.

---

## 제외할 사이트도 정리했다

### SunSirs

기술적으로는 수집이 가능했다.

하지만 이용약관이 없었다.
존재한다는 흔적은 찾았는데 원문을 찾지 못했다.

사회는 기술보다 법이 더 무섭다.

얌전히 제외했다.

### 국내 축산 전문지 4곳

축산신문, 축산경제신문, 농수축산신문, 농업축산신문은 모두 무단전재 및 재배포 금지 조항이 있었고, 일부는 AI 학습 이용 금지까지 명시되어 있었다.

소스가 갑자기 우르르 나와서 좀 신났었는데....

---

## GDELT도 조금 더 알아봤다

공식 문서를 뒤져봤다.

rate limit은 있었다.

근데 숫자는 안 알려준다.

- 아저씨 여기 어떻게 가는지 아세요?
- 알아.

그래도 몇 가지는 확인할 수 있었다.

```python
# gdelt_collector.py

MAX_RETRIES = 4
BACKOFF_BASE_SECONDS = 60  # 60 -> 120 -> 240 -> 480초로 증가 (누적 900초)

# 일시적 네트워크 에러(ConnectTimeout 등) 재시도 횟수/대기시간.
# 429(rate limit)와 ConnectTimeout이 같은 실행 안에서 섞여 나오는 걸 확인함.
# 이건 요청 제한과는 별개로, 순간적인 접속 실패일 가능성이 높아 429보다는 훨씬 짧게 재시도.
NETWORK_ERROR_MAX_RETRIES = 2
NETWORK_ERROR_WAIT_SECONDS = 10
```

* 공식적으로 요청 제한이 존재한다는 점
* 실사용 사례에서는 짧은 시간에 요청이 몰리면 약 15분 정도 차단되는 경우가 있다는 점(공식은 아님)
* TIMESPAN 파라미터 형식
* ConnectTimeout은 429와 별개로 처리하는 편이 맞겠다는 점

공식 문서보다 남의 삽질 기록이 더 도움이 되는군.

---

## 퇴근

오늘은 기능을 많이 만든 날은 아니었다.

대신 프로젝트의 방향을 꽤 많이 정리했다.

특히 종합 랭킹을 없애기로 한 결정이 가장 컸다. 억지로 해결하려고 붙잡고 있기보다, 문제 자체를 없애는 편이 훨씬 깔끔했다.
여전히 좀 아쉽긴 하지만... 달리 방법이 보이지 않는다.

코드는 생각보다 많이 늘지 않았다.

대신 문서는 엄청 늘었다.
그것도 좀... 과하게.

...이것도 개발 맞겠지.

---

## 다음에 할 일

* WATTAgNet, Feed Strategy, Feed & Grain 구조가 정말 같은지 직접 확인하기
* GitHub Actions에서 크롤링 차단이 있는지 테스트하기
* GDELT 429 대응 더 다듬기

오늘도 하나 결정하면 두 개가 새로 생겼다.

언젠가는 TODO가 줄어드는 날도 오겠지.

...오겠지?