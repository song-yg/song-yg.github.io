---
title: "개발일지 #5 - 버그버그버그"
date: 2026-07-20 21:30:00 +0900
categories: [개발일지, 사료축산뉴스봇]
tags: [python, llm, github-actions, gdelt, 디버깅]
---


사실 어제 로그 읽다가 버그 잔뜩 찾았는데...

다 줄줄줄 적어만 놓고 손대지는 않았었음.

그래서 오늘은 하루종일 버그만 잡음...

근데 그와중에 깃허브가 죽었음...

<br>

대가리 빠게질 것 같다.

<br>

---

## GitHub 죽음

워크플로가 5분째 러너 배정도 안 되고 멈춰 있길래...

워크플로우 문제인줄 알고 클로드를 겁나 갈궜다.

<br>

![github actions 다운](/assets/img/posts/2026-07-20-devlog/01-git-actionissue.png){: width="600" .normal }
_붉다, 모든 것이 붉다._

근데 그냥 깃허브가 터진거였음.

<br>

진짜 안타까운게...

나야 출근하고 확인했지만, 미국은 새벽이었음.

12시부터 3시까지 저거 고치고 계셨음...!!

<br>

---

## LLM도 죽음

내가 멀쩡히 만들면 뭐하나

외부에서 다죽는데

```
요약: User Safety: safe
```

테스트 동안 오픈라우터에서 무료 api 모델을 쓰고 있는데

요약이 아니라 안전성 판정 결과가 그대로 온 거였다...

<br>

```python
# llm_summarizer.py
def _is_suspicious_summary(text: str) -> bool:
    return "user safety" in text.lower()
```

그래서 저것만 배척해봤다.

모델 바꾸면 또 수정해야 될수도...

<br>

---

## 구글 스프레드 시트 연동

나는 한달 반의 짦은 단기 인턴이고,

이 회사에는 아마도? 개발자분이 따로 없는 듯 하다.

~~계셨으면 내 사수분으로 배정되지 않았을까...~~

<br>

그래서 내가 퇴사한 뒤에도 제대로 유지관리가 되어야하는데...

음, 파일 수정하고 터미널 열어서 깃허브에 로그인 하고 푸쉬하고 테스트까지 돌려보는걸...

비전공자가... 할 수 있나...?

이왕 하는거 좀 안정적으로 만드는게 좋을텐데...

<br>

```python
# keyword_source.py
def get_keywords(lang: str, fallback: list[str]) -> list[str]:
    csv_url = os.environ.get("KEYWORD_SHEET_CSV_URL")
    if not csv_url:
        print(f"[keyword_source] KEYWORD_SHEET_CSV_URL 없음 - {lang} 기본(하드코딩) 키워드 리스트 사용: {fallback}")
        return fallback

    rows = _fetch_csv_rows(csv_url)
    if rows is None:
        return fallback

    keywords = [
        row["keyword"].strip()
        for row in rows
        if row.get("lang", "").strip().lower() == lang
        and _is_active(row)
        and row.get("keyword", "").strip()
    ]
    ...
```

그래서 스프레드 시트를 연동했다.

일단은 내 계정에 해놨는데, 나중에 회사쪽으로 옮기면 될 듯.

<br>

![github actions 다운](/assets/img/posts/2026-07-20-devlog/04-data-keyword.png){: width="600" .normal }

표는 대충 이렇게 생겼다.

다들 익숙하게 사용하실 수 있을 듯!

<br>

나중에 버그 메뉴얼도 만들어야 하는데,

... 그냥 버그날 일이 좀 없었으면 좋겠다 진심

---

## 카테고리 : 기타

로그를 보니까 카테고리에서 분류되지 못한게 과반수를 넘었다.

이놈들은 뭐지...

그렇다고 수백건에 달하는 기사를 다 출력하라고 하기도 좀...

<br>

좀 그렇지만...

<br>

해야 한다...

<br>

아직 제대로 살펴보지는 않았지만, WATT쪽 키워드가 제대로 등록되지 않은 듯 하다.

배포 직전 즈음에 우다다 추가하면 될 듯.

일단은 넘겼다.

<br>

---

## 중복 검색

`feed cost`랑 `feed costs`가 모두 키워드로 등록되어 있었다.

~~심지어 이런게 27쌍이었음~~

키워드는 그냥 클로드한테 맞겨놓고 대충 검수했었는데...

그래도 지금이라도 발견해서 다행이다.

하마타면 2배 이벤트 할 뻔.

<br>

```python
# keyword_tagger.py
def _dedupe_contained(terms: list[str]) -> list[str]:
    result = []
    for term in terms:
        term_lower = term.lower()
        if any(term != other and term_lower in other.lower() for other in terms):
            continue
        result.append(term)
    return result
```

클로드한테 시켰더니 이렇게 짜줬다.

음... 일단은 괜찮아보임.

<br>

---

## gdelt 상한선

키워드 좀 늘려서 테스트 해봤더니

바로 상한선을 찍어버렸다...

<br>

시계열은 키워드당 api를 각각 호출하는데,

단순 데이터는 여러개를 한번에 호출하는 형식이다.

그런데 키워드 좀 늘렸더니 최대 상한 250개를 넘었다.

<br>

이거 구조 고민하느라 고민을 좀... 많이 했다.

키워드 각각 호출하자니 시간이 기하급수적으로 늘어날 가능성이 있고,

그렇다고 묶어서 하자니, 이번이랑 비슷한 문제가 생길 것 같고...

그래서 클로드한테 SOS를 외쳐봤는데

<br>

***"알아서 판단하게 하죠!"***

<br>

...

그게 되는건가?

<br>

```python
# gdelt_collector.py
def _detect_crowded_keywords(articles: list[dict], keywords: list[str]) -> list[str]:
    total = len(articles)
    if total < MAX_RECORDS * CROWDING_CAP_TRIGGER_RATIO:
        return []

    crowders = []
    for keyword in keywords:
        keyword_lower = keyword.lower()
        count = sum(1 for a in articles if keyword_lower in a["title"].lower())
        if count / total >= CROWDING_SHARE_THRESHOLD:
            crowders.append(keyword)
    return crowders
```

되는구나...

<br>

처음에는 묶어서 돌리다가, 초과하면 각각 돌리는 방향으로 정했다.

<br>

---

## 또 시계열임

진짜 gdelt 때문에 진절머리가 난다.

시계열... 이게 진짜 좋은 데이터인데...

429가 너무 많이 떠서 부담이 너무 크다!

<br>

그래서 `timelinevol`(비율)이랑 `timelinevolraw`(실제 건수) 중 뭐가 오래 걸리는지 테스트를 해봤다.

처음에는 `timelinevol`은 12.5초 만에 끝났고 `timelinevolraw`는 429 백오프를 4단계 다 쓰고 16분 만에 실패했다.

그래서 후자만 버리는 쪽으로 갈까 했는데...

테스트는 더 돌려보는게 맞을 것 같았음.

아니나 다를까, 두번째는 바로 전자가 429 먹음;;

<br>


```python
# test_timeline_timing.py
for idx, keyword in enumerate(TEST_KEYWORDS):
    order = MODES if idx % 2 == 0 else list(reversed(MODES))
    call_order_used.append(" -> ".join(order))
    ...
    for mode in order:
        ...
```

아예 앞뒤 순서 바꿔서도 테스트 하려한다.

오늘은 이제 곧 퇴근이고... 내일 마저 해봐야지.

<br>

---

## 퇴근

오늘은 진짜 뭐 버그만 잡다가 하루가 끝났다.

초반에 깃허브 서버 문제로 테스트를 못돌려서 시간이 잠깐 뜬 것도 있고.

그래도 살펴볼수록 완성도는 높아져서 다행이다.

---

## 다음에 할 일

* 시계열 처리 방침 최종 결정 (빼기 / 유지 / 절충)
* 시계열 두 모드 순서 교차 테스트, 표본 더 쌓기
* 적응형 배치 잠정값(배치 크기, 크라우딩 임계값) 실측 튜닝
* v4 키워드 시트 구글 시트에 실제 반영
* 저장 레이어(5단계)는 여전히 손도 안 댐

내일은 테스트만 돌리고 있을 듯.
